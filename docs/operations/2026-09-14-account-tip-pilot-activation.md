# Devnet pilot activation: migration 0021 and gate enablement

Source: original BRUH project at exact main `1544fd1afbd97295e71a087659d223e74c2aacc8` (merge of PR68, reviewed head `898681188d3596934fb975f3f29412b7b1acca94`). Working tree clean; no source implementation, website marketing, layout or copy edits.

## Migration

`docs/proposed-account-tip-pilot-controls.sql` applied verbatim (semantics unchanged) as managed migration `drizzle/migrations/0021_bruh_account_tip_pilot_controls.sql`. The outer `begin`/`commit` were omitted only because the managed runner wraps each migration in its own transaction. Journal history preserved; migrations 0018-0020 were not reapplied and their semantics and grants are untouched. Generated database types were regenerated by the runner. No seed data was inserted.

Schema effects: `public.bruh_account_tip_execution` gains nullable `next_reconcile_at timestamptz`; new table `public.bruh_account_tip_rpc_budget(day date primary key, checks integer not null check(checks between 1 and 500))`; new function `public.bruh_account_tip_reconcile_claim(uuid,bigint)`.

## Access-control evidence

Budget table `public.bruh_account_tip_rpc_budget`: `relrowsecurity=true`, `relforcerowsecurity=true`, policies `0`, direct SELECT/INSERT/UPDATE/DELETE grants for PUBLIC/anon/authenticated/service_role: none. Row count `0`.

Function ACL and definition (`pg_proc`):

| function | owner | security definer | search_path | service_role | anon | authenticated | PUBLIC |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `bruh_account_tip_reconcile_claim` | postgres | true | `pg_catalog` | true | false | false | false |
| `bruh_account_tip_finalize_credit` | postgres | true | `pg_catalog` | true | false | false | false |
| `bruh_account_tip_finalize` | postgres | true | `pg_catalog` | false | false | false | false |
| `bruh_account_tip_signed` | postgres | true | `pg_catalog` | false | false | false | false |

Limitation recorded previously still applies: the managed platform's shared `postgres` owner and the platform `sandbox_exec` read role are outside these application-role denials, so these checks are application-role evidence, not host-level isolation evidence.

## Checks

- TypeScript typecheck: passed.
- Vitest: 173 tests across 32 files passed.
- Production build: passed.
- Disposable PostgreSQL validation `tools/db-validation/account-tip-credit.pg.mjs` (real credit/community SQL plus cooldown and global-budget claim paths): 1/1 passed.

## Configuration

Set for the devnet pilot: `BRUH_ACCOUNT_TIPS_DEVNET_ENABLED=true`, `BRUH_ACCOUNT_SIGNING_DEVNET_ENABLED=true`. Unchanged: `SOLANA_NETWORK=devnet`, `BRUH_ACCOUNT_WALLETS_DEVNET_ENABLED=true`, wrapping key and key version, Helius and Telegram credentials. No mainnet, reward, swap, export, retirement, withdrawal or scheduler change.

## Provider verification

One production `readDevnetAccount` `getBalance` request at finalized commitment for the public disposable devnet address `3hwWdq42GCvBubMNbLV9ysBozGTjQMG76Dnic2EBJQHn`. The transport enforced devnet genesis `EtWTRABZaYq6iMfeYKouRu166VU2xqa1wcaWoxPkrZBG` before the read. Result: successful devnet transport, balance `4966935000` lamports, context slot present. No endpoint or key value is recorded. No faucet, mainnet, signing, sending, wallet provisioning or password reset occurred.

Not published; Codex reviews and publishes under standing owner authorization.
