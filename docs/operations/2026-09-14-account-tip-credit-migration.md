# Account-tip credit migration (managed Cloud)

Source: original BRUH Cloud project `e287f314`, GitHub main `24f1d0fb994daf6578abf66b0160e62c993c3fa8`
(merge of PR #67). Working tree clean; no source edits.

## Applied migration

| Journal entry | Source file |
| --- | --- |
| `drizzle/migrations/0020_bruh_account_tip_credit.sql` | `docs/proposed-account-tip-credit.sql` |

SQL semantics preserved verbatim except the outer `begin;`/`commit;`, which the managed runner supplies by
wrapping each migration in its own transaction. Migrations 0015-0019 were not reapplied; journals and
history preserved. Generated Supabase types were regenerated by the runner. No seed rows, no grant
relaxation, no credential values.

## Underlying table compatibility (verified before apply)

`public.tip_intents`: `id uuid`, `group_id uuid`, `network text`, `sender_membership_id uuid`,
`recipient_membership_id uuid`, `recipient_address text`, `asset_symbol text`, `asset_mint text NULL`,
`amount_base_units bigint`, `amount_display numeric`, `reference_key text`, `privacy tip_privacy`,
`status intent_status`, `expires_at timestamptz`, `created_at timestamptz`. Enum labels used by the
function exist: `tip_privacy.public`, `intent_status.confirmed`.

`public.verified_transfers`: `tip_intent_id uuid`, `signature text`, `slot bigint NULL`,
`recipient_address text`, `asset_mint text NULL`, `amount_base_units bigint`, `confirmed_at timestamptz`,
`raw jsonb`. No incompatibility found; no schema was invented or altered.

## Verified facts after apply

`public.bruh_account_tip_credit_audit`:
- `relrowsecurity = true`, `relforcerowsecurity = true`.
- `has_table_privilege(SELECT)` false for `anon`, `authenticated`, `service_role`;
  `has_table_privilege(INSERT)` false for `service_role`.
- Row count 0.
- Immutable trigger `bruh_account_tip_credit_immutable` on UPDATE/DELETE.

`public.bruh_account_tip_finalize_credit(uuid,bigint,text,bigint,bigint)`:
- owner `postgres`, `SECURITY DEFINER = true`, `search_path = pg_catalog`.
- EXECUTE: `service_role` true; `anon`, `authenticated`, `PUBLIC` false.

`public.bruh_account_tip_finalize(uuid,bigint,text,bigint,bigint)`:
- EXECUTE for `service_role` is now false — the foundation finalizer is reachable only through the
  credit wrapper.

## Configuration

`BRUH_ACCOUNT_TIPS_DEVNET_ENABLED` and `BRUH_ACCOUNT_SIGNING_DEVNET_ENABLED` remain absent (false).
`SOLANA_NETWORK=devnet`. Wrapping-key, Helius and Telegram secrets untouched. No scheduler, webhook,
mainnet, rewards, swap or publish action. No wallet provisioning and no signed transfer.

## Checks

- Typecheck: passed.
- Tests: 171 passed across 32 files.
- Production build: passed.
- Disposable PostgreSQL validation `tools/db-validation/account-tip-credit.pg.mjs` (includes the actual
  community leaderboard SQL): 1/1 passed — single credit per confirmed tip, retry cannot double count,
  forced audit failure rolls back both legacy rows and finalization, forgotten membership removed from
  the public projection.

## Known limitations

- Every managed object is owned by the shared `postgres` superuser role, which can alter or drop any of
  these objects; the deny grants constrain application roles, not the platform owner.
- The platform `sandbox_exec` role holds BYPASSRLS and read ACLs, so forced RLS does not hide these rows
  from platform-level administration.
- Shared-worker encrypted custody remains in force: no isolated signer runtime, and no reset, export,
  withdrawal or retirement flow. Devnet tip and signing gates remain off, so no tip has been credited.
