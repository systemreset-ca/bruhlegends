# Guardian connection validation — 2026-09-13

Purpose: connect the owner-authorized funded-wallet design to the isolated signer without copying elevated backend credentials or wallet envelopes into BRUH.

BRUH source `b801d366dcc20168281e64d63e5f879eb16510c2` adds the dormant receiver factory and a gateway-to-receiver contract exercise. Thirty targeted tests passed across service authentication, gateway authorization and receiver boundaries. The actual service signatures are ephemeral Ed25519 keys; Telegram and wallet callbacks are fixtures. They do not establish a real Telegram account, deployed cross-project request, persistent wallet or on-chain transfer. Receiver source `400fa34a5dff899091b646a6d172feda97e65dd0` passed exact GitHub CI. Type checking and receiver lint also passed locally.

The full application suite passed 191 tests across 26 files and the production Worker build passed. The client output contained no references to the caller secret configuration, bridge signing scheme/function or custody gateway module. Record-only source `8ac5dd9b45c7601f3e639f3c3670ec7828a71ba2` passed [exact GitHub CI](https://github.com/systemreset-ca/bruhlegends/actions/runs/34790876387).

Guardian source `01d3311198fe0a5010d5dd166e975355adaaed47` was reviewed: receiver/verifier imports were adapted, the existing post-authorization provisioning logic was extracted into a shared server-only scope core, and the default-disabled fixed POST route uses pinned Telegram verification and only BRUH's public service key. The core retains envelope authentication and first-writer-wins checks; production wrapping/store getters remain unconfigured. Guardian's reported offline validation includes 102 receiver assertions and 51 protocol-contract assertions, not real-account evidence. Latest `6299faac295ca0367871dd54fe70501c16dcea2b` adds only documentation; that reviewed source was published with the bounded diagnostic flag enabled for one read-only trial, while the wallet bridge remains disabled. The diagnostic must be republished off immediately after the result.

The receiver accepts only the fixed provisioning POST, exact version-one four-key payload and explicitly pinned BRUH public verification key. Its durable nonce is consumed exactly once. Guardian independently verifies Telegram initData and binds the user/chat to the signed membership approval created from BRUH's own database. Its provision callback receives only immutable devnet scope; wallet material stays in Guardian. Strict public replies reject envelope extensions, unfrozen accounts and mainnet. Missing enable/configuration/policy/storage/wrapping authority fails closed.

Guardian's repository is public under the owner's explicit visibility authorization after a fresh redacted scan of 112 commits found no leaks. Its deployed diagnostics were independently checked returning 404 and no-store after shutdown. Offline Worker crypto previously passed; the first deployed devnet RPC trial did not obtain genesis. Guardian status-only metadata source `9198e5e065197177cc418bd3ee79ee117c7fea47` was reviewed and assigned for one bounded diagnostic trial: only HTTP status numbers, counts, timeout/transport flags and fixed classification, never provider error text or credentials.

A separate direct public devnet genesis read returned the exact expected full hash `EtWTRABZaYq6iMfeYKouRu166VU2xqa1wcaWoxPkrZBG`. Thus no network-identifier change is justified by the Worker failure. Local/provider reads do not establish Worker transport compatibility.

The bounded deployed trial at Guardian `6299faac295ca0367871dd54fe70501c16dcea2b` accepted signed authentication and rejected unauthorized/replay requests, but returned three of ten checks with one outbound attempt, zero HTTP responses/statuses, no timeout and transport_failure. No provider ban, rate-limit response or credential requirement can be inferred. Codex published the off flag and independently verified both diagnostic POSTs and wallet provisioning POST return 404/no-store.

Source inspection identified a Worker host-function binding risk: the HTTP adapter stored native global fetch and invoked it as a method of the adapter. Default transport is now an explicit arrow invoking `globalThis.fetch(input, init)`. Fourteen HTTP tests pass, including a branded-fetch mock that requires the global receiver. Guardian is applying the same binding to its HTTP adapter and its diagnostic's extracted default fetch. This source test does not prove the cause of the deployed failure; another bounded live trial is required before RPC compatibility is claimed.

The subsequent live binding-fix trial still returned transport_failure with one attempt, zero responses/statuses, no timeout and illegalInvocation=false under the narrow classifier. Auth/replay checks passed. Codex published the off flag again. The binding fix alone therefore did not resolve transport; Guardian is investigating known exception fingerprints inside its backend without emitting raw error text. Do not substitute a mocked/local success or request owner credentials on a guessed provider cause.

`services/custody-signer/wrapping-provider.ts` adds a source-only isolated vault loader, denied unless explicitly enabled with a version and owning provider callback. The callback supplies an exclusive mutable copy of a persistent random 32-byte wrapping key; the loader imports a nonextractable AES-GCM key and clears that copy. It creates no boot/demo/replacement key, pads/truncates nothing and never stores key material. Five tests pass with ephemeral fixtures: fresh-factory envelope reopening, disabled/missing configuration, invalid keys, wrong key/version and opaque provider failure. This is not Cloud secret configuration, actual process-restart evidence or a production key. The loader must never be configured in BRUH's application backend; Guardian retains wrapping authority.

Guardian source integration is assigned against the exact receiver commit. Its private shared provisioning core must retain the existing structural validation, authenticated envelope decryption, scope/race winner checks and immutable audit. Do not create an HMAC bypass or reuse diagnostic secrets. No wallet schema, real user wallet, funding or mainnet activation is authorized by this evidence record. Custody remains draft PR 46; production `/start` remains the existing external-wallet flow until the replacement is ready.
