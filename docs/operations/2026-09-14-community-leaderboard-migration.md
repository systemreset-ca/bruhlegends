# Community leaderboard Cloud migration — applied

Source: `systemreset-ca/bruhlegends` main `6ea951c79a5a3e4e56c55f18afe02bc513ee862a` (PR #61). Backend only; no website copy, layout, brand or route changes. Secrets and configuration untouched.

## Applied migration

- `drizzle/migrations/0017_bruh_community_leaderboard.sql` — next free journal entry, existing journals and history preserved.
- Contents are `docs/proposed-community-leaderboard.sql` verbatim except the outer `begin;`/`commit;`, omitted because the managed runner wraps each migration in its own transaction.
- No tables created, no source-table grants, no RLS changes, no seed or fixture rows.
- `src/integrations/supabase/types.ts` regenerated by the managed migration runner.

## Function and privilege evidence

`pg_proc` inspection of `public.bruh_community_leaderboard(text,integer,text,text)`:

| Property | Value |
| --- | --- |
| owner | `postgres` |
| `prosecdef` (SECURITY DEFINER) | `true` |
| `proconfig` | `search_path=pg_catalog` |
| EXECUTE `service_role` | `true` |
| EXECUTE `anon` | `false` |
| EXECUTE `authenticated` | `false` |
| EXECUTE `PUBLIC` | `false` |

Platform limit recorded honestly: the read-only SQL session role is neither `service_role` nor able to `SET ROLE service_role`, so direct SQL invocation returned `42501 permission denied for function`. That denial is itself evidence the grant is not broad. Invocation was therefore exercised through the service-role server reader `getCommunityLeaderboard`.

## Invocation counts (devnet, limit 10)

Row counts only; no user rows, display names, wallet addresses or group identifiers are recorded here.

| Kind | Window | Rows |
| --- | --- | --- |
| callers | all | 1 |
| callers | 7d | 1 |
| callers | 30d | 1 |
| tippers | all | 0 |
| tippers | 7d | 0 |
| tippers | 30d | 0 |

Tipper rankings are empty because no confirmed public devnet tip has a matching verified transfer yet.

## Checks

- `bun run typecheck` (tsc --noEmit): passed.
- `bunx vitest run`: 148 tests in 28 files passed, including `community-leaderboard`, `community-commands` and `community-auth`.
- Production build: passed.

Not published. Codex reviews the backend diff before publication. No spending, signing, export, retirement, mainnet, rewards, scheduler or account-tip schema work is included.
