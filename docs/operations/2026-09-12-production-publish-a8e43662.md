# Production publish: devnet safety release

Operation date: 2026-09-12 at approximately 14:37 America/Toronto.

GitHub and production baseline: `a8e43662d7613cb69de4cd2c2dc52530dc2a19d7`.

Lovable published the current GitHub `main` release candidate to the connected `bruh.tips` and `www.bruh.tips` domains. The release includes the Solana devnet safety gate, verified Helius devnet configuration, accurate public token-status copy, and a stable lint policy for the two Supabase integration files regenerated by Lovable.

## Pre-publish evidence

- Production build passed.
- Strict TypeScript checking passed.
- All 78 tests across twelve files passed.
- The preview footer read `bruh.tips · bruh-legends.xyz · Solana · Non-custodial`.
- The public token copy described SOL tipping as under devnet validation, USDC as network-gated, and BRUH as not minted and disabled.
- `SOLANA_NETWORK` was classified as devnet, the mainnet gate remained closed, and no BRUH mint was configured.

## Post-publish smoke checks

- `GET https://bruh.tips/` returned HTTP 200 and contained the BRUH Legends brand.
- `GET https://bruh.tips/token` returned HTTP 200 and contained the corrected devnet-validation copy.
- An unauthenticated `POST https://bruh.tips/api/public/telegram/webhook` returned HTTP 401, confirming the route remained live and rejected a request without the webhook secret.

No secret value, wallet key, seed phrase, transaction signature or private payload was read, displayed or committed. This release remains a devnet validation build and does not establish mainnet or real-funds readiness.
