# Release evidence template — not an actual release

- Date/time and operator:
- Target/domain:
- Source commit and merge PR:
- Previous deployed commit:
- Linked issues/milestone/decision:
- Behavior changed:
- Application tests/typecheck/lint/build:
- Database/concurrency/live-user checks (`not run` when absent):
- Applied migration identifiers:
- Configuration names changed (never values):
- Public deployment probes and results:
- On-chain references, network and reconciliation where applicable:
- Remaining limitations and gates:
- Rollback/pause procedure:

Verify the actual deployed source; a merge, successful build or template is not publication evidence. Never publish private Telegram identities or secret-bearing provider URLs.
